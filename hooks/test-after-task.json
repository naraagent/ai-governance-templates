{
  "name": "Run Tests After Task",
  "version": "1.0.0",
  "description": "Automatically run test suite after each spec task completes",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": "npm test"
  }
}
