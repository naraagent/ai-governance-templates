{
  "name": "Lint TypeScript on Save",
  "version": "1.0.0",
  "description": "Run ESLint with auto-fix when TypeScript files are saved in ECS Hub services",
  "when": {
    "type": "fileEdited",
    "patterns": ["*.ts", "*.tsx"]
  },
  "then": {
    "type": "runCommand",
    "command": "npx eslint --fix ${file}"
  }
}
