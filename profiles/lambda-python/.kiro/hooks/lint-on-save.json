{
  "name": "Python Lint on Save",
  "version": "1.0.0",
  "description": "Run ruff linter when Python files are saved",
  "when": {
    "type": "fileEdited",
    "patterns": ["*.py"]
  },
  "then": {
    "type": "runCommand",
    "command": "ruff check --fix ${file} && ruff format ${file}"
  }
}
